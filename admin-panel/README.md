# FounderOS Admin Panel

This is a separate dashboard for managing the FounderOS ecosystem.

## Setup
1. Open a terminal in this directory (`cd admin-panel`).
2. Run `npm install`.
3. Run `npm run dev` to start the dashboard on port 3002.

## Features
- Real-time system metrics
- User management & search
- Workspace oversight
- Invitation tracking
